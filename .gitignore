.env

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# uv / venv
.venv/

# tool caches
.pytest_cache/
.ruff_cache/

# throwaway probe scripts — not part of the repo
scratch/

# per-checkout Docker Compose override (see compose.override.yml.example)
compose.override.yml

# Deciduous database (local)
.deciduous/

# Harness runtime artifacts (PID file, server logs)
harness/alpha-server.pid
harness/alpha-server.log
harness/embed-server.pid
harness/embed-server.log
